<Application x:Class="CutMaker.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#181D26" />
        <SolidColorBrush x:Key="MutedBrush" Color="#9DA9BD" />
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Microsoft JhengHei UI, Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#EAF0FA" />
            <Setter Property="Background" Value="#11151D" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#293348" />
            <Setter Property="Foreground" Value="#EAF0FA" />
            <Setter Property="BorderBrush" Value="#3B4961" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Border" Property="Background" Value="#374961" /></Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Border" Property="BorderBrush" Value="#78DCCB" /></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="GridSplitter">
            <Setter Property="Background" Value="#2F394C" />
            <Setter Property="ShowsPreview" Value="True" />
            <Setter Property="Focusable" Value="True" />
            <Setter Property="KeyboardIncrement" Value="12" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#10151E" />
            <Setter Property="Foreground" Value="#EAF0FA" />
            <Setter Property="BorderBrush" Value="#3B4961" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="CaretBrush" Value="#78DCCB" />
        </Style>
        <Style TargetType="ToolTip">
            <Setter Property="MaxWidth" Value="650" />
            <Setter Property="ContentTemplate">
                <Setter.Value><DataTemplate><TextBlock Text="{Binding}" TextWrapping="Wrap" /></DataTemplate></Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
